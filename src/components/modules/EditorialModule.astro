---
import type { CollectionEntry } from 'astro:content';
import SectionMeta from '../SectionMeta.astro';
import Infographic from '../Infographic.astro';
type Props = { data: CollectionEntry<'traditions'>['data'] };
const { data } = Astro.props;
---

<div class="editorial reveal">
  <div class="prose">
    <slot />
  </div>
  {data.infographic && <Infographic {...data.infographic} />}
  <SectionMeta data={data} />
</div>

<style>
  .editorial {
    max-width: var(--measure);
    margin: 0 auto;
  }
  .prose {
    font-size: clamp(1.15rem, 1rem + 0.7vw, 1.4rem);
    line-height: 1.75;
    font-weight: 400;
  }
  .prose :global(p) {
    margin-bottom: 1.4rem;
  }
  .prose :global(p:last-child) {
    margin-bottom: 0;
  }
  .prose :global(strong) {
    color: var(--accent);
    font-weight: 600;
  }
  .prose :global(em) {
    font-style: italic;
    color: color-mix(in srgb, var(--text) 88%, var(--accent));
  }
</style>
